<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Linux SPI设备如何从Device Tree走到driver probe</title>
    <link href="/2026/08/01/Linux%20Kernel/SPI/Linux%20SPI%E8%AE%BE%E5%A4%87%E5%A6%82%E4%BD%95%E4%BB%8EDevice%20Tree%E8%B5%B0%E5%88%B0driver%20probe/"/>
    <url>/2026/08/01/Linux%20Kernel/SPI/Linux%20SPI%E8%AE%BE%E5%A4%87%E5%A6%82%E4%BD%95%E4%BB%8EDevice%20Tree%E8%B5%B0%E5%88%B0driver%20probe/</url>
    
    <content type="html"><![CDATA[]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>SPI</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux SPI</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Linux SPI Framework整体架构分析</title>
    <link href="/2026/08/01/Linux%20Kernel/SPI/Linux%20SPI%E6%95%B4%E4%BD%93%E6%9E%B6%E6%9E%84/"/>
    <url>/2026/08/01/Linux%20Kernel/SPI/Linux%20SPI%E6%95%B4%E4%BD%93%E6%9E%B6%E6%9E%84/</url>
    
    <content type="html"><![CDATA[<h1 id="1-SPI-Framework"><a href="#1-SPI-Framework" class="headerlink" title="1. SPI Framework"></a>1. SPI Framework</h1><p>上一章我们讲解了SPI的物理模型和传输时序图，从这一章开始我们深入分析Linux SPI框架。整个框架如图：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.7axvl7557d.webp"></p><p>看到这张图是不是蒙了？先别急，后面我们会慢慢讲解。你只需要记住，核心就分为三个层次：</p><ul><li><strong>第一层</strong>：设备驱动层（Device Driver），主要负责设备的功能，简单的理解就是跟 SoC 通信的设备</li><li><strong>第二层</strong>：SPI Core 层，这是 Linux 提供的公共框架，负责管理每次的 SPI 通信请求</li><li><strong>第三层</strong>：SPI Controller Driver，负责具体的 SoC 的 SPI 硬件</li></ul><hr><h1 id="2-三个核心对象"><a href="#2-三个核心对象" class="headerlink" title="2. 三个核心对象"></a>2. 三个核心对象</h1><p>整个框架里面有三个核心对象，就是软件层抽象的物理世界。分别是 <code>spi_controller</code>、<code>spi_device</code> 和 <code>spi_driver</code>。其中：</p><ul><li><code>spi_controller</code> 表示一台 SPI 主控制器</li><li><code>spi_device</code> 表示挂在该控制器上的一个从设备</li><li><code>spi_driver</code> 是对应的从设备的软件驱动</li></ul><p>结构关系：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.41yrok92si.webp"></p><h2 id="2-1-spi-controller"><a href="#2-1-spi-controller" class="headerlink" title="2.1 spi_controller"></a>2.1 spi_controller</h2><p>我们先来看 <code>spi_controller</code>，其在物理层对应 Master，源码位置在 <code>include/linux/spi/spi.h</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_controller</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">device</span> <span class="hljs-title">dev</span>;</span>              <span class="hljs-comment">/* 内嵌的设备结构体，用于挂载到设备模型 */</span><br><br>    <span class="hljs-type">int</span> (*transfer)(...);           <span class="hljs-comment">/* 批量传输：一次可发起多个 spi_message */</span><br><br>    <span class="hljs-type">int</span> (*transfer_one)(...);       <span class="hljs-comment">/* 单次传输：一次只处理一个 spi_message */</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>我们先不分析源码，先有个概念：它表示一个SPI控制器，SPI控制器就是 SoC 里面对应的能实现SPI通信的端口。比如一块 SoC 里面可能有 SPI0~SPI2 三个控制器，那么每一个就对应软件层的一个 <code>spi_controller</code>。主要负责产生时钟信号、控制片选CS、数据收发等。</p><h2 id="2-2-spi-device"><a href="#2-2-spi-device" class="headerlink" title="2.2 spi_device"></a>2.2 spi_device</h2><p>接着看 <code>spi_device</code>，对应从机设备。比如你有一个 IMU，那么在 SPI 控制框架中就被抽象成一个 <code>spi_device</code> 对象。源码位置在 <code>include/linux/spi/spi.h</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_device</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">device</span> <span class="hljs-title">dev</span>;</span>                  <span class="hljs-comment">/* 内嵌的设备结构体，用于挂载到设备模型 */</span><br><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_controller</span> *<span class="hljs-title">controller</span>;</span>  <span class="hljs-comment">/* 指向所属的 SPI 主控制器 */</span><br><br>    u32 max_speed_hz;                   <span class="hljs-comment">/* 该从设备支持的最大通信时钟频率 (Hz) */</span><br><br>    u8 mode;                            <span class="hljs-comment">/* SPI 工作模式 (CPOL/CPHA 组合，如 MODE0~3) */</span><br><br>    u8 chip_select;                     <span class="hljs-comment">/* 片选信号编号 (对应主控制器上的第几个 CS 引脚) */</span><br><br>&#125;;<br></code></pre></td></tr></table></figure><h2 id="2-3-spi-driver"><a href="#2-3-spi-driver" class="headerlink" title="2.3 spi_driver"></a>2.3 spi_driver</h2><p>有了设备描述还不够，还需要对应的驱动代码，也就是 <code>spi_driver</code>，源码也在 <code>include/linux/spi/spi.h</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_driver</span> &#123;</span><br>    <span class="hljs-type">const</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_device_id</span> *<span class="hljs-title">id_table</span>;</span>   <span class="hljs-comment">/* 支持的设备ID表，用于与 spi_device 匹配 */</span><br>    <span class="hljs-type">int</span>         (*probe)(<span class="hljs-keyword">struct</span> spi_device *spi);   <span class="hljs-comment">/* 设备匹配成功后调用的探测函数 */</span><br>    <span class="hljs-type">int</span>         (*remove)(<span class="hljs-keyword">struct</span> spi_device *spi);   <span class="hljs-comment">/* 设备移除时调用的清理函数 */</span><br>    <span class="hljs-type">void</span>        (*shutdown)(<span class="hljs-keyword">struct</span> spi_device *spi); <span class="hljs-comment">/* 系统关机时调用的回调 */</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">device_driver</span>    <span class="hljs-title">driver</span>;</span>          <span class="hljs-comment">/* 内嵌的通用设备驱动结构体 */</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>不同设备的 <code>spi_driver</code> 不一样，填充上面的结构体即可。</p><hr><h1 id="3-DTS-创建-spi-device"><a href="#3-DTS-创建-spi-device" class="headerlink" title="3. DTS 创建 spi_device"></a>3. DTS 创建 spi_device</h1><p>这个我们直接以一个实例感受：</p><figure class="highlight dts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs dts"><span class="hljs-title class_">spi0</span> <span class="hljs-punctuation">&#123;</span><br><br>    <span class="hljs-attr">status</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;okay&quot;</span><span class="hljs-punctuation">;</span><br><br><br>    <span class="hljs-title class_">imu@0</span> <span class="hljs-punctuation">&#123;</span><br><br>        <span class="hljs-attr">compatible</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;invensense,icm42688&quot;</span><span class="hljs-punctuation">;</span><br><br><br>        <span class="hljs-attr">reg</span> <span class="hljs-operator">=</span> <span class="hljs-params">&lt;<span class="hljs-number">0</span>&gt;</span><span class="hljs-punctuation">;</span><br><br><br>        <span class="hljs-attr">spi-max-frequency</span> <span class="hljs-operator">=</span> <span class="hljs-params">&lt;<span class="hljs-number">10000000</span>&gt;</span><span class="hljs-punctuation">;</span><br><br>    <span class="hljs-punctuation">&#125;;</span><br><span class="hljs-punctuation">&#125;;</span><br></code></pre></td></tr></table></figure><p>上面表示在 SPI0 控制器下面挂载了一个 IMU 从设备。其中：</p><ul><li><code>reg = &lt;0&gt;</code>：表示 CS 编号，这里就是 CS0</li><li><code>spi-max-frequency</code>：最大时钟频率，这里是 10MHz</li><li><code>status</code>：使能</li><li><code>compatible</code>：与驱动中 <code>spi_driver</code> 匹配</li></ul><p>整个启动流程就是先解析设备树，把参数给到 SPI Controller Driver，创建 <code>spi_device</code> 后与对应的 <code>spi_driver</code> 进行匹配，匹配完成后执行 <code>probe</code> 函数。</p><hr><h1 id="4-SPI-数据传输流程"><a href="#4-SPI-数据传输流程" class="headerlink" title="4. SPI 数据传输流程"></a>4. SPI 数据传输流程</h1><p>假设传感器驱动读取2字节：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c">spi_read(spi, rx_buf, <span class="hljs-number">2</span>);<br><br><span class="hljs-type">static</span> <span class="hljs-keyword">inline</span> <span class="hljs-type">int</span><br><span class="hljs-title function_">spi_read</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> spi_device *spi, <span class="hljs-type">void</span> *buf, <span class="hljs-type">size_t</span> len)</span><br>&#123;<br>    <span class="hljs-comment">/* 构造一个 spi_transfer 描述本次传输 */</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_transfer</span> <span class="hljs-title">t</span> =</span> &#123;<br>            .rx_buf     = buf,      <span class="hljs-comment">/* 接收缓冲区，存放从设备发来的数据 */</span><br>            .len        = len,      <span class="hljs-comment">/* 本次传输的字节数 */</span><br>        &#125;;<br><br>    <span class="hljs-comment">/* 发起同步传输：阻塞等待传输完成，最后一个参数表示 transfer 个数 */</span><br>    <span class="hljs-keyword">return</span> spi_sync_transfer(spi, &amp;t, <span class="hljs-number">1</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>其中的 <code>spi</code> 表示 <code>struct spi_device *spi</code> 具体的设备。其构造了一个 <code>spi_transfer</code>，随后放入 <code>spi_message</code>，然后同步传输。其中 <code>spi_message</code> 表示一次完整的逻辑通信。接着，SPI Core 把消息交给对应 <code>spi_controller</code>，SPI Core 从 message 中取出 transfer，然后调用 <code>transfer_one()</code>。示意图如下：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.2vfgg3dtw9.webp"></p><hr><h1 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h1><p>好了，到这里我们就结束了。Linux SPI框架主要通过分层设计，将SPI设备驱动与SPI控制器硬件实现进行解耦。</p><p>整体分为三个部分：</p><ul><li><strong>spi_driver</strong>：负责设备功能</li><li><strong>SPI Core</strong>：负责设备管理和数据调度</li><li><strong>spi_controller driver</strong>：负责控制具体的SPI硬件</li></ul><p>当设备树创建SPI设备后，Linux通过 driver match 机制调用设备驱动 <code>probe</code> 函数，随后设备驱动通过SPI Core提供的接口完成数据传输。</p><hr><h1 id="6-互动"><a href="#6-互动" class="headerlink" title="6. 互动"></a>6. 互动</h1><ol><li>Linux 为什么需要 SPI Framework？</li><li>一个 SPI 设备从 DTS 到驱动 probe 的流程是什么？</li><li>spi_controller、spi_device、spi_driver 三者是什么关系？</li><li>数据传输时，数据如何从设备驱动走到底层硬件？</li></ol>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>SPI</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux SPI</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>SPI协议基础：从四根线理解Linux SPI框架</title>
    <link href="/2026/08/01/Linux%20Kernel/SPI/SPI%E5%8D%8F%E8%AE%AE%E5%9F%BA%E7%A1%80/"/>
    <url>/2026/08/01/Linux%20Kernel/SPI/SPI%E5%8D%8F%E8%AE%AE%E5%9F%BA%E7%A1%80/</url>
    
    <content type="html"><![CDATA[<h1 id="1-What-is-SPI"><a href="#1-What-is-SPI" class="headerlink" title="1. What is SPI?"></a>1. What is SPI?</h1><p>在开始本章之前，先抛出一个问题，Linux SPI 框架最终要控制什么？</p><p>在嵌入式系统中，核心就是CPU，只有CPU是不能工作的，往往还需要接入各种外设并且与外设通信。<br>例如传感器、Flash、显示屏、ADC等。不同设备之间需要一种统一的数据交换方式，因此产生了各种通信协议，例如 UART、I2C、SPI。<br>本系列就是介绍其中的SPI。</p><p>先给出定义：SPI（Serial Peripheral Interface）是一种<strong>高速</strong>、<strong>全双工</strong>、<strong>同步串行</strong>通信协议。</p><p>这里针对三个关键点分别解释：</p><p><strong>1. 高速</strong></p><p>高速是一个模糊概念，究竟高到什么程度？这里高速相对于IIC，普通UART，CAN等低速总线。本身并没有规定最高速度。<br>其传输数据的速率和SPI的时钟频率有关：</p><table><thead><tr><th align="right">SPI 时钟</th><th align="right">理论位速率</th></tr></thead><tbody><tr><td align="right">1 MHz</td><td align="right">1 Mbit&#x2F;s</td></tr><tr><td align="right">10 MHz</td><td align="right">10 Mbit&#x2F;s</td></tr><tr><td align="right">20 MHz</td><td align="right">20 Mbit&#x2F;s</td></tr><tr><td align="right">50 MHz</td><td align="right">50 Mbit&#x2F;s</td></tr><tr><td align="right">100 MHz</td><td align="right">100 Mbit&#x2F;s</td></tr></tbody></table><p><strong>2. 全双工</strong></p><p>与之对应的还有两个概念：单工和半双工。<br>画个图大家就清楚了：</p><ul><li><p>单工：数据始终只能单向传输</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.4ubn5re1ld.webp"></p></li><li><p>半双工：同一时间只允许向一个方向传输</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.7lkpdu6mlz.webp"></p></li><li><p>全双工：同一个时刻允许两台同时进行双向数据传输</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.102vmtfrlh.webp"></p></li></ul><p><strong>3. 同步串行</strong></p><p>很明显，分两部分理解。</p><p>先看<strong>串行</strong>，就是数据一位一位的传输。例如传输数据为一个字节：10110010。<br>串行通信只能使用一根数据线。每个时刻只能传输一位。<br>与之对应的为<strong>并行</strong>，如果使用八根传输线，同一个时刻能传输一个字节。</p><p>再看<strong>同步</strong>，现在数据线上有数据了，那我接收的时候怎么确定什么时候开始读取呢？<br>又怎么知道每一位持续多长时间？<br>同步通信通过一根时钟线解决这个问题，就像打电话的铃声，通知你该接电话了。</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.lwfvzeqnm.webp"></p><p>时钟信号如上，我们只需要规定数据在哪个时钟边沿发生变化，以及在哪个时钟边沿被读取就行。</p><hr><h1 id="2-Why-Need-SPI"><a href="#2-Why-Need-SPI" class="headerlink" title="2. Why Need SPI?"></a>2. Why Need SPI?</h1><p>通过上面的讲解，我觉得大家应该知道SPI是个什么东西了。那为什么需要这个东西？不是还有其他协议吗？你SPI就特殊些？就只有你能干了？</p><p>其实不是，众多协议中没有一种是最优的。只是每种协议都有自己的偏向性，SPI 是其中偏向”短距离、高速度、简单直接”的方案。</p><p>SPI通常由四根线组成，分别为：</p><ul><li>SCLK：时钟</li><li>MOSI：主机发、从机收</li><li>MISO：从机发、主机收</li><li>CS：片选</li></ul><p>只要主机选中设备就能进行传输。</p><p>那为什么不用UART代替呢？UART 适合两个设备进行简单的点对点通信，对速率要求不高。</p><p>那为什么不用IIC代替呢？IIC只需两个引脚，牺牲了物理那么软件实现就相对复杂。</p><ul><li>设备多、速度低、希望省线就选 I²C</li><li>设备少、数据多、追求速度就选 SPI</li></ul><p>其他协议主播还没工程经验，就不讲了，后面有了再补充。</p><hr><h1 id="3-SPI-物理结构"><a href="#3-SPI-物理结构" class="headerlink" title="3. SPI 物理结构"></a>3. SPI 物理结构</h1><p>接下来让我们看一下SPI的物理结构。上面其实已经讲了，SPI一般由四根线组成：</p><ul><li>SCLK：时钟</li><li>MOSI（Master Output Slave Input）：主机发、从机收</li><li>MISO（Master Input Slave Output）：从机发、主机收</li><li>CS：片选</li></ul><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.6il030xecw.webp"></p><p>但有的时候会有5根的情况，是增加了一条 READY 线。之所以需要是因为SPI有一个限制：只有主机可以产生时钟，从机不能主动发起 SPI 传输。<br>假设从机完成了一次数据采集，想把数据发给主机。它不能直接通过 MISO 发送，因为此时没有 SCLK。上面我们已经说过SCLK的作用和重要性了。此时就需要这根线去通知主机，在两边做约束。</p><p>那什么时候需要 READY 呢？如果从机只需要被主机查询，用标准四根线；如果从机需要及时通知主机”我有事&#x2F;有数据&#x2F;准备好了”，通常加一根 READY&#x2F;IRQ，使用五根线。</p><p>我们思考一个场景，假设主机产生信号，告诉从机你马上响应我的需求，如果这个时候从机能立马响应且完成需求，那么4根线也没什么关系。如果不能立刻响应，那么主机就需要轮询地问”你好了吗？你好了吗？快点，你好了吗？”</p><p>轮询会造成什么恶果呢？一个是占用CPU，一个是响应延迟。那么此时 READY 线就有用了。</p><hr><h1 id="4-SPI-通信时序"><a href="#4-SPI-通信时序" class="headerlink" title="4. SPI 通信时序"></a>4. SPI 通信时序</h1><h2 id="4-1-通信模式"><a href="#4-1-通信模式" class="headerlink" title="4.1 通信模式"></a>4.1 通信模式</h2><p>简单的讲就是两边共同的约定。主要有4种模式 Mode0~3，由两个参数决定：CPOL 和 CPHA。</p><p><strong>CPOL（时钟极性）</strong>：表示不通信时，SCLK 保持高电平还是低电平。</p><ul><li><p>当 CPOL &#x3D; 0 时，空闲电平为低</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.102vmzeyun.webp"></p></li><li><p>当 CPOL &#x3D; 1 时，空闲电平为高</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.2ksmmgcwjl.webp"></p></li></ul><p><strong>CPHA（时钟相位）</strong>：一个时钟周期中，在哪个边沿采样数据。</p><ul><li>当 CPHA &#x3D; 0 时，每个周期的第一个边沿采样</li><li>当 CPHA &#x3D; 1 时，每个周期的第二个边沿采样</li></ul><h2 id="4-2-实例"><a href="#4-2-实例" class="headerlink" title="4.2 实例"></a>4.2 实例</h2><p>我们以一个实际的例子讲解通讯过程中的时序图。<br>假设主机要读取从机 0x12 寄存器，从机返回数据 0x5A。</p><p>两边做一下规定：</p><ul><li>使用 SPI Mode 0，即 CPOL&#x3D;0，CPHA&#x3D;0</li><li>8位数据</li><li>MSB first，高位先传</li><li>读命令最高位为1</li><li>寄存器地址为 0x12</li><li>主机发送命令 0x80 | 0x12，由操作码和寄存器地址组成</li><li>主机再发送一个 Dummy 字节 0xFF，为从机返回数据提供时钟</li></ul><p>整个数据链：</p><table><thead><tr><th></th><th align="right">第一字节</th><th align="right">第二字节</th></tr></thead><tbody><tr><td>主机发送</td><td align="right">0x92</td><td align="right">0xFF</td></tr><tr><td>从机返回</td><td align="right">无效</td><td align="right">0x5A</td></tr></tbody></table><p>整个时序图如下：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.6m4m0vcjlj.webp"></p><p>主机先拉低CS，选中从机。根据设置的 Mode0，在每个周期的第一个边沿采样数据。<br>产生8个时钟，在MOSI上发送 0x92，发送完成后，接着发送 0xFF 为从机提供时钟，从机收到 0x92 的信息去寄存器 0x12 取数据，在MISO上返回 0x5A。</p><h2 id="4-3-为什么-Linux-需要-SPI-框架"><a href="#4-3-为什么-Linux-需要-SPI-框架" class="headerlink" title="4.3 为什么 Linux 需要 SPI 框架"></a>4.3 为什么 Linux 需要 SPI 框架</h2><p>为什么Linux需要一个统一的SPI框架？现实的场景往往不是一个MCU对应一个固定的SPI，而是不同厂商不同类型的SPI外设。而SPI框架的作用就是把控制器怎么工作和外设需要传输什么数据分离，并统一管理。</p><p>整个框架如下，先大致了解一下，后面会详细介绍：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.1sfr4ura3k.webp"></p><ul><li><strong>Device Driver</strong>：决定具体的通信设备是什么</li><li><strong>SPI Core</strong>：负责管理每次的SPI通信请求</li><li><strong>SPI Controller Driver</strong>：负责具体的硬件操作，比如使用什么模式、数据的传输宽度、SPI寄存器配置等</li><li><strong>Hardware</strong>：具体硬件</li></ul><hr><h1 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h1><p>SPI是一种同步串行通信协议，通过SCK、MOSI、MISO、CS四根信号线实现Master和Slave之间的数据交换。</p><p>在Linux系统中，SPI硬件被抽象为SPI Controller，而具体外设被抽象为SPI Device。Linux SPI Framework通过SPI Core连接Controller Driver和Device Driver，使设备驱动无需关注底层寄存器操作，只需要调用统一SPI接口即可完成数据传输。</p><hr><h1 id="6-互动"><a href="#6-互动" class="headerlink" title="6. 互动"></a>6. 互动</h1><p>你现在能回答下面四个问题吗？欢迎在评论区交流讨论：</p><ol><li>SPI数据谁产生？</li><li>Linux SPI驱动框架里面谁对应Master？</li><li>为什么需要Linux SPI驱动框架？</li></ol>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>SPI</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux SPI</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Linux SPI核心数据结构分析</title>
    <link href="/2026/08/01/Linux%20Kernel/SPI/Linux%20SPI%E6%A0%B8%E5%BF%83%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%88%86%E6%9E%90/"/>
    <url>/2026/08/01/Linux%20Kernel/SPI/Linux%20SPI%E6%A0%B8%E5%BF%83%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%88%86%E6%9E%90/</url>
    
    <content type="html"><![CDATA[<h1 id="1-spi-controller"><a href="#1-spi-controller" class="headerlink" title="1. spi_controller"></a>1. spi_controller</h1><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.7axvl7557d.webp"></p><p>之前我们讲解了整个SPI框架的构成和数据传输流程。现在我们深入源码分析每一部分。</p><p>首先来看一下 <code>spi_controller</code>，它表示一个 SoC 内部的 SPI 控制器硬件，对应物理模型的 Master。<br>比如一片 SoC 里面有 SPI0-3 控制器，那么对应的就是 <code>spi_controller0-3</code>。</p><p>源码位置：<code>include/linux/spi/spi.h</code></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_controller</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">device</span> <span class="hljs-title">dev</span>;</span>              <span class="hljs-comment">// 内嵌的设备模型，用于挂载到 Linux 设备树</span><br><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">list</span>;</span>          <span class="hljs-comment">// 将所有 spi_controller 链接到全局链表</span><br><br>    s16 bus_num;                    <span class="hljs-comment">// 控制器编号，如 SoC 中 SPI0 对应 bus_num = 0</span><br>    u16 num_chipselect;             <span class="hljs-comment">// 该控制器最多支持多少个片选信号(CS)</span><br><br>    <span class="hljs-comment">/* 控制器能力的位掩码 */</span><br>    u32 mode_bits;                  <span class="hljs-comment">// 支持的 SPI 模式位(CPOL/CPHA/CS_HIGH 等)</span><br>    u32 bits_per_word_mask;         <span class="hljs-comment">// 支持的字长掩码，如 8bit、16bit</span><br><br>    <span class="hljs-comment">/* 时钟频率范围 */</span><br>    u32 min_speed_hz;               <span class="hljs-comment">// 支持的最小 SCK 频率</span><br>    u32 max_speed_hz;               <span class="hljs-comment">// 支持的最大 SCK 频率</span><br><br>    <span class="hljs-comment">/* 控制器硬件特性标志 */</span><br>    u16 flags;<br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_CONTROLLER_HALF_DUPLEX  BIT(0)  <span class="hljs-comment">// 只支持半双工</span></span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_CONTROLLER_NO_RX        BIT(1)  <span class="hljs-comment">// 不支持接收</span></span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_CONTROLLER_NO_TX        BIT(2)  <span class="hljs-comment">// 不支持发送</span></span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_CONTROLLER_MUST_RX      BIT(3)  <span class="hljs-comment">// 必须同时接收</span></span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_CONTROLLER_MUST_TX      BIT(4)  <span class="hljs-comment">// 必须同时发送</span></span><br><br>    <span class="hljs-type">bool</span> slave;                     <span class="hljs-comment">// 是否为从机控制器(极少数场景)</span><br><br>    <span class="hljs-comment">/* 传输大小限制(可选) */</span><br>    <span class="hljs-type">size_t</span> (*max_transfer_size)(<span class="hljs-keyword">struct</span> spi_device *spi);<br>    <span class="hljs-type">size_t</span> (*max_message_size)(<span class="hljs-keyword">struct</span> spi_device *spi);<br><br>    <span class="hljs-comment">/* ---- 锁与同步 ---- */</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">mutex</span> <span class="hljs-title">io_mutex</span>;</span>          <span class="hljs-comment">// I/O 互斥锁，保证同一时刻只有一个传输在进行</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">mutex</span> <span class="hljs-title">add_lock</span>;</span>          <span class="hljs-comment">// 防止重复添加相同 CS 的锁</span><br><br>    <span class="hljs-comment">/* ---- 回调函数集 ---- */</span><br>    <span class="hljs-type">int</span> (*setup)(<span class="hljs-keyword">struct</span> spi_device *spi);             <span class="hljs-comment">// 配置从设备的模式和时钟</span><br>    <span class="hljs-type">int</span> (*transfer)(<span class="hljs-keyword">struct</span> spi_device *spi,<br>                    <span class="hljs-keyword">struct</span> spi_message *mesg);        <span class="hljs-comment">// 旧式批量传输接口(已不推荐)</span><br>    <span class="hljs-type">void</span> (*cleanup)(<span class="hljs-keyword">struct</span> spi_device *spi);          <span class="hljs-comment">// 释放设备资源</span><br><br>    <span class="hljs-comment">/* ---- 新版排队传输机制 ----</span><br><span class="hljs-comment">     * 若设置 queued = true，则使用 kthread_worker 将 message 排队处理，</span><br><span class="hljs-comment">     * 不再直接调用 transfer()。</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-type">bool</span> queued;                           <span class="hljs-comment">// 是否启用排队传输</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">kthread_worker</span> *<span class="hljs-title">kworker</span>;</span>        <span class="hljs-comment">// 内核线程，负责处理消息队列</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">kthread_work</span> <span class="hljs-title">pump_messages</span>;</span>     <span class="hljs-comment">// 消息泵工作项</span><br>    <span class="hljs-type">spinlock_t</span> queue_lock;                 <span class="hljs-comment">// 队列自旋锁</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">queue</span>;</span>                <span class="hljs-comment">// 待处理的消息链表</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_message</span> *<span class="hljs-title">cur_msg</span>;</span>           <span class="hljs-comment">// 当前正在处理的消息</span><br>    <span class="hljs-type">bool</span> busy;                             <span class="hljs-comment">// 控制器是否繁忙</span><br>    <span class="hljs-type">bool</span> running;                          <span class="hljs-comment">// 控制器是否处于运行状态</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">completion</span> <span class="hljs-title">xfer_completion</span>;</span>     <span class="hljs-comment">// 传输完成通知量</span><br><br>    <span class="hljs-comment">/* 新版回调(控制器驱动实现这些) */</span><br>    <span class="hljs-type">int</span> (*prepare_transfer_hardware)(<span class="hljs-keyword">struct</span> spi_controller *ctlr);  <span class="hljs-comment">// 准备硬件(如使能时钟)</span><br>    <span class="hljs-type">int</span> (*unprepare_transfer_hardware)(<span class="hljs-keyword">struct</span> spi_controller *ctlr); <span class="hljs-comment">// 释放硬件</span><br>    <span class="hljs-type">int</span> (*prepare_message)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                           <span class="hljs-keyword">struct</span> spi_message *message);            <span class="hljs-comment">// 消息预处理</span><br>    <span class="hljs-type">int</span> (*unprepare_message)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                             <span class="hljs-keyword">struct</span> spi_message *message);          <span class="hljs-comment">// 消息后处理</span><br>    <span class="hljs-type">int</span> (*transfer_one_message)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                                <span class="hljs-keyword">struct</span> spi_message *mesg);          <span class="hljs-comment">// 处理一条消息</span><br>    <span class="hljs-type">int</span> (*transfer_one)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                        <span class="hljs-keyword">struct</span> spi_device *spi,<br>                        <span class="hljs-keyword">struct</span> spi_transfer *transfer);             <span class="hljs-comment">// 处理单个 transfer(最常用)</span><br>    <span class="hljs-type">void</span> (*set_cs)(<span class="hljs-keyword">struct</span> spi_device *spi, <span class="hljs-type">bool</span> enable);            <span class="hljs-comment">// 控制片选电平</span><br>    <span class="hljs-type">void</span> (*handle_err)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                       <span class="hljs-keyword">struct</span> spi_message *message);                <span class="hljs-comment">// 传输错误处理</span><br><br>    <span class="hljs-comment">/* ---- 片选 GPIO ---- */</span><br>    <span class="hljs-type">int</span> *cs_gpios;                     <span class="hljs-comment">// 片选 GPIO 编号数组</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">gpio_desc</span> **<span class="hljs-title">cs_gpiods</span>;</span>      <span class="hljs-comment">// 片选 GPIO 描述符数组</span><br>    <span class="hljs-type">bool</span> use_gpio_descriptors;         <span class="hljs-comment">// 是否使用 GPIO 描述符管理 CS</span><br><br>    <span class="hljs-comment">/* ---- DMA 支持 ---- */</span><br>    <span class="hljs-type">bool</span> (*can_dma)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                    <span class="hljs-keyword">struct</span> spi_device *spi,<br>                    <span class="hljs-keyword">struct</span> spi_transfer *xfer);   <span class="hljs-comment">// 判断当前传输是否可用 DMA</span><br>    <span class="hljs-type">size_t</span> max_dma_len;                           <span class="hljs-comment">// 单次 DMA 最大传输长度</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dma_chan</span> *<span class="hljs-title">dma_tx</span>;</span>                      <span class="hljs-comment">// DMA 发送通道</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">dma_chan</span> *<span class="hljs-title">dma_rx</span>;</span>                      <span class="hljs-comment">// DMA 接收通道</span><br>    <span class="hljs-type">void</span> *dummy_rx;                               <span class="hljs-comment">// 全双工时无数据要收时的占位缓冲区</span><br>    <span class="hljs-type">void</span> *dummy_tx;                               <span class="hljs-comment">// 全双工时无数据要发时的占位缓冲区</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>我们看一下源码，只看重要部分，先看最重要的。</p><h2 id="1-1-transfer-one"><a href="#1-1-transfer-one" class="headerlink" title="1.1 transfer_one"></a>1.1 transfer_one</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span> (*transfer_one)(<span class="hljs-keyword">struct</span> spi_controller *ctlr,<br>                    <span class="hljs-keyword">struct</span> spi_device *spi,<br>                    <span class="hljs-keyword">struct</span> spi_transfer *transfer);<br></code></pre></td></tr></table></figure><p>表示让每个硬件完成一次传输。它不是一个固定函数，而是提供给 SPI Core 的回调接口。</p><p>那它是怎么被调用的呢？前面我们说过了，会在 SPI Core 层通过下面函数调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">controller-&gt;transfer_one()<br></code></pre></td></tr></table></figure><p>SPI Core 不知道具体 SPI 控制器的寄存器结构，而是提供了上面的统一接口。具体控制器驱动会在初始化时赋值。</p><p>我们以 RK 系列为例，提供了 <code>spi-rockchip.c</code>，具体链接我会提供在末尾。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">rockchip_spi_transfer_one</span><span class="hljs-params">(</span><br><span class="hljs-params">        <span class="hljs-keyword">struct</span> spi_controller *ctlr,</span><br><span class="hljs-params">        <span class="hljs-keyword">struct</span> spi_device *spi,</span><br><span class="hljs-params">        <span class="hljs-keyword">struct</span> spi_transfer *xfer)</span><br></code></pre></td></tr></table></figure><p>其中定义的属于这款 SoC 独特的 <code>transfer_one</code>。</p><p>在 probe 函数中会被调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">rockchip_spi_probe</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> platform_device *pdev)</span> &#123;<br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_controller</span> *<span class="hljs-title">ctlr</span>;</span><br><br>    ctlr-&gt;transfer_one = rockchip_spi_transfer_one;<br><br>&#125;<br></code></pre></td></tr></table></figure><p>以后 SPI Core 调用：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">ctlr-&gt;transfer_one(ctlr, spi, transfer);<br></code></pre></td></tr></table></figure><p>实际上调用的是：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">rockchip_spi_transfer_one(ctlr, spi, transfer);<br></code></pre></td></tr></table></figure><p>整个调用关系如下：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.7i13hjunxg.webp"></p><p>这就是整个 Linux 驱动框架里面典型的<strong>抽象层 + 回调机制</strong>，读者可以慢慢体会一下。</p><h2 id="1-2-num-chipselect"><a href="#1-2-num-chipselect" class="headerlink" title="1.2 num_chipselect"></a>1.2 num_chipselect</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">u16 num_chipselect;             <span class="hljs-comment">// 该控制器最多支持多少个片选信号(CS)</span><br></code></pre></td></tr></table></figure><p>注释上写了，我就不啰嗦了。</p><h2 id="1-3-bus-num"><a href="#1-3-bus-num" class="headerlink" title="1.3 bus_num"></a>1.3 bus_num</h2><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">s16 bus_num;                    <span class="hljs-comment">// 控制器编号，如 SoC 中 SPI0 对应 bus_num = 0</span><br></code></pre></td></tr></table></figure><p>对应的控制器编号。</p><hr><h1 id="2-spi-device"><a href="#2-spi-device" class="headerlink" title="2. spi_device"></a>2. spi_device</h1><p>设备端，就是具体的从机设备，挂在某个 SPI 控制器上的一个外设。<br>源码位置：<code>include/linux/spi/spi.h</code></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_device</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">device</span> <span class="hljs-title">dev</span>;</span>                    <span class="hljs-comment">// 内嵌的设备模型，用于挂载到 Linux 设备树</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_controller</span> *<span class="hljs-title">controller</span>;</span>    <span class="hljs-comment">// 指向所属的 SPI 主控制器</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_controller</span> *<span class="hljs-title">master</span>;</span>        <span class="hljs-comment">// 旧名称，兼容层，等同于 controller</span><br>    u32 max_speed_hz;                     <span class="hljs-comment">// 该从设备支持的最大 SPI 时钟频率 (Hz)</span><br>    u8 chip_select;                       <span class="hljs-comment">// 片选信号索引（对应控制器第几路 CS）</span><br>    u8 bits_per_word;                     <span class="hljs-comment">// 每个字的位数（如 8、16）</span><br>    u32 mode;                             <span class="hljs-comment">// SPI 工作模式（CPOL/CPHA/CS_HIGH 等组合）</span><br>    <span class="hljs-type">int</span> irq;                              <span class="hljs-comment">// 该设备使用的中断号</span><br>    <span class="hljs-type">void</span> *controller_state;               <span class="hljs-comment">// 控制器驱动私有数据（控制器驱动读写）</span><br>    <span class="hljs-type">void</span> *controller_data;                <span class="hljs-comment">// 板级额外配置数据（设备树/平台数据传入）</span><br>    <span class="hljs-type">char</span> modalias[SPI_NAME_SIZE];         <span class="hljs-comment">// 设备名，用于与 spi_driver 匹配</span><br>    <span class="hljs-type">const</span> <span class="hljs-type">char</span> *driver_override;          <span class="hljs-comment">// 强制绑定的驱动名（用于 sysfs 手动匹配）</span><br>    <span class="hljs-type">int</span> cs_gpio;                          <span class="hljs-comment">// 旧版片选 GPIO 编号（已不推荐）</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">gpio_desc</span> *<span class="hljs-title">cs_gpiod</span>;</span>           <span class="hljs-comment">// 片选 GPIO 描述符（新版方式）</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">word_delay</span>;</span>          <span class="hljs-comment">// 字间延迟（两个 word 之间的等待时间）</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">cs_setup</span>;</span>            <span class="hljs-comment">// CS 拉低后到第一个 CLK 之间的延时</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">cs_hold</span>;</span>             <span class="hljs-comment">// 最后一个 CLK 后到 CS 拉高之间的延时</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">cs_inactive</span>;</span>         <span class="hljs-comment">// CS 拉高后再次拉低的最小间隔</span><br>&#125;;<br></code></pre></td></tr></table></figure><h2 id="2-1-chip-select"><a href="#2-1-chip-select" class="headerlink" title="2.1 chip_select"></a>2.1 chip_select</h2><p>对应片选 CS，比如设备树：</p><figure class="highlight dts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs dts"><span class="hljs-title class_">imu@0</span> <span class="hljs-punctuation">&#123;</span><br><br>    <span class="hljs-attr">reg</span> <span class="hljs-operator">=</span> <span class="hljs-params">&lt;<span class="hljs-number">0</span>&gt;</span><span class="hljs-punctuation">;</span><br><br><span class="hljs-punctuation">&#125;;</span><br></code></pre></td></tr></table></figure><p>则 <code>chip_select = 0</code>。</p><h2 id="2-2-mode"><a href="#2-2-mode" class="headerlink" title="2.2 mode"></a>2.2 mode</h2><p>SPI 的模式，对应 CPOL + CPHA 的组合。</p><hr><h1 id="3-spi-driver"><a href="#3-spi-driver" class="headerlink" title="3. spi_driver"></a>3. spi_driver</h1><p>设备对应的驱动代码：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_driver</span> &#123;</span><br>    <span class="hljs-type">const</span> <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_device_id</span> *<span class="hljs-title">id_table</span>;</span>   <span class="hljs-comment">// 支持的设备 ID 表，用于与 spi_device 的 modalias 匹配</span><br>    <span class="hljs-type">int</span>         (*probe)(<span class="hljs-keyword">struct</span> spi_device *spi);       <span class="hljs-comment">// 设备匹配成功后调用，初始化硬件并注册</span><br>    <span class="hljs-type">int</span>         (*remove)(<span class="hljs-keyword">struct</span> spi_device *spi);      <span class="hljs-comment">// 设备移除时调用，释放资源</span><br>    <span class="hljs-type">void</span>        (*shutdown)(<span class="hljs-keyword">struct</span> spi_device *spi);    <span class="hljs-comment">// 系统关机/重启时调用</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">device_driver</span>    <span class="hljs-title">driver</span>;</span>                       <span class="hljs-comment">// 内嵌的设备驱动模型，包含 name、of_match_table 等</span><br>&#125;;<br></code></pre></td></tr></table></figure><p>最重要的就是 <code>probe</code> 函数，当 device 和 driver 匹配后会调用。</p><p><code>probe</code> 函数通常完成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">probe</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> spi_device *spi)</span><br>&#123;<br><br>    spi_setup(spi);<br><br><br>    spi_read();<br><br>    register_device();<br><br>&#125;<br></code></pre></td></tr></table></figure><hr><h1 id="4-spi-transfer"><a href="#4-spi-transfer" class="headerlink" title="4. spi_transfer"></a>4. spi_transfer</h1><p>一次连续 SPI 数据交换，是 SPI 传输的最小单位，表示<strong>一次不可分割的、连续的字节流传输</strong>。一个 <code>spi_message</code> 可以包含多个 <code>spi_transfer</code>，靠链表串联。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_transfer</span> &#123;</span><br>    <span class="hljs-type">const</span> <span class="hljs-type">void</span> *tx_buf;             <span class="hljs-comment">// 发送缓冲区指针（要发给从设备的数据）</span><br>    <span class="hljs-type">void</span> *rx_buf;                   <span class="hljs-comment">// 接收缓冲区指针（存放从设备返回的数据）</span><br>    <span class="hljs-type">unsigned</span> len;                   <span class="hljs-comment">// 本次传输的字节数</span><br><br>    <span class="hljs-comment">/* DMA 相关 */</span><br>    <span class="hljs-type">dma_addr_t</span> tx_dma;              <span class="hljs-comment">// 发送缓冲区的 DMA 地址</span><br>    <span class="hljs-type">dma_addr_t</span> rx_dma;              <span class="hljs-comment">// 接收缓冲区的 DMA 地址</span><br><br>    <span class="hljs-comment">/* 传输模式（单线/双线/四线） */</span><br>    <span class="hljs-type">unsigned</span> tx_nbits:<span class="hljs-number">3</span>;            <span class="hljs-comment">// 发送数据线数：1bit / 2bit / 4bit</span><br>    <span class="hljs-type">unsigned</span> rx_nbits:<span class="hljs-number">3</span>;            <span class="hljs-comment">// 接收数据线数：1bit / 2bit / 4bit</span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_NBITS_SINGLE  0x01  <span class="hljs-comment">// 1 线传输（标准 SPI）</span></span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_NBITS_DUAL    0x02  <span class="hljs-comment">// 2 线传输（Dual SPI）</span></span><br>    <span class="hljs-meta">#<span class="hljs-keyword">define</span> SPI_NBITS_QUAD    0x04  <span class="hljs-comment">// 4 线传输（Quad SPI）</span></span><br><br>    <span class="hljs-comment">/* 控制标志 */</span><br>    <span class="hljs-type">unsigned</span> dummy_data:<span class="hljs-number">1</span>;          <span class="hljs-comment">// 是否为占位数据（全双工时一方无实际数据时用）</span><br>    <span class="hljs-type">unsigned</span> cs_change:<span class="hljs-number">1</span>;           <span class="hljs-comment">// 本次 transfer 结束后是否翻转片选</span><br><br>    u8 bits_per_word;               <span class="hljs-comment">// 每个字的位数（如 8、16），可覆盖 spi_device 的默认值</span><br><br>    <span class="hljs-comment">/* 时序延迟 */</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">delay</span>;</span>         <span class="hljs-comment">// 本次 transfer 结束后的延时</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">cs_change_delay</span>;</span> <span class="hljs-comment">// cs_change 生效后的等待时间</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_delay</span> <span class="hljs-title">word_delay</span>;</span>    <span class="hljs-comment">// 两个数据字之间的间隔</span><br><br>    u32 speed_hz;                   <span class="hljs-comment">// 本次传输用的时钟频率（可覆盖默认值）</span><br><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">transfer_list</span>;</span> <span class="hljs-comment">// 链表节点，挂载到 spi_message.transfers</span><br>&#125;;<br></code></pre></td></tr></table></figure><h2 id="4-1-tx-buf-rx-buf-len"><a href="#4-1-tx-buf-rx-buf-len" class="headerlink" title="4.1 tx_buf &#x2F; rx_buf &#x2F; len"></a>4.1 tx_buf &#x2F; rx_buf &#x2F; len</h2><p>三件套，描述一次传输数据。</p><p>比如发送：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">tx_buf</span>：<span class="hljs-number">0</span>x11 <span class="hljs-number">0</span>x22<br></code></pre></td></tr></table></figure><p>接收：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">rx_buf</span>：<span class="hljs-number">0</span>x11 <span class="hljs-number">0</span>x22<br></code></pre></td></tr></table></figure><p>那么长度 <code>len = 2</code>。</p><h2 id="4-2-transfer-list"><a href="#4-2-transfer-list" class="headerlink" title="4.2 transfer_list"></a>4.2 transfer_list</h2><p>链表节点，用来将多个 <code>spi_transfer</code> 串在一起，挂到 <code>spi_message.transfers</code> 上。SPI Core 在 <code>spi_transfer_one_message()</code> 中通过 <code>list_for_each_entry</code> 遍历这个链表，逐个交给 <code>controller-&gt;transfer_one()</code>。</p><hr><h1 id="5-spi-message"><a href="#5-spi-message" class="headerlink" title="5. spi_message"></a>5. spi_message</h1><p>表示一次完整的 SPI 事件。比如先发送地址再读取数据，这两个动作组成一个 message。<br>源码位置：<code>include/linux/spi/spi.h</code></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_message</span> &#123;</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">transfers</span>;</span>         <span class="hljs-comment">// 链表头，串联多个 spi_transfer</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">spi_device</span> *<span class="hljs-title">spi</span>;</span>             <span class="hljs-comment">// 指向本次通信的目标从设备</span><br>    <span class="hljs-type">unsigned</span> is_dma_mapped:<span class="hljs-number">1</span>;           <span class="hljs-comment">// 缓冲区是否已经做过 DMA 映射</span><br><br>    <span class="hljs-comment">/* 异步完成回调 */</span><br>    <span class="hljs-type">void</span> (*complete)(<span class="hljs-type">void</span> *context);    <span class="hljs-comment">// 传输完成时调用的回调函数</span><br>    <span class="hljs-type">void</span> *context;                      <span class="hljs-comment">// 传给 complete 回调的参数</span><br><br>    <span class="hljs-comment">/* 传输结果 */</span><br>    <span class="hljs-type">unsigned</span> frame_length;              <span class="hljs-comment">// 预计传输的总字节数</span><br>    <span class="hljs-type">unsigned</span> actual_length;             <span class="hljs-comment">// 实际传输的字节数</span><br>    <span class="hljs-type">int</span> status;                         <span class="hljs-comment">// 传输结果状态（0 成功，负值表示错误码）</span><br><br>    <span class="hljs-comment">/* 内部使用 */</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">queue</span>;</span>             <span class="hljs-comment">// 消息在控制器等待队列中的节点</span><br>    <span class="hljs-type">void</span> *state;                        <span class="hljs-comment">// 控制器驱动私有状态（可选）</span><br>    <span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">list_head</span> <span class="hljs-title">resources</span>;</span>         <span class="hljs-comment">// spi_res 资源链表，传输完成后自动释放</span><br>&#125;;<br></code></pre></td></tr></table></figure><p><strong>1. <code>transfers</code></strong></p><p>将若干个 <code>spi_transfer</code> 通过链表串起来，控制器驱动按顺序逐个发送。</p><p><code>spi_message</code> 与 <code>spi_transfer</code> 的关系如下：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.45idna9pwm.webp"></p><blockquote><p><strong><code>spi_transfer</code> 是数据传输的最小段，<code>spi_message</code> 将多个段组合成一次片选持续的完整事务，设备驱动通过构造 <code>spi_message</code> 并发起 <code>spi_sync()</code> &#x2F; <code>spi_async()</code> 来完成与芯片的交互。</strong></p></blockquote><hr><h1 id="6-一次-SPI-传输的数据流"><a href="#6-一次-SPI-传输的数据流" class="headerlink" title="6. 一次 SPI 传输的数据流"></a>6. 一次 SPI 传输的数据流</h1><p>整个流程我们在上一章讲解过了，这里就不多说了，回顾一下：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.2vfgg3dtw9.webp"></p><hr><h1 id="7-总结"><a href="#7-总结" class="headerlink" title="7. 总结"></a>7. 总结</h1><p>Linux SPI Framework 通过多个核心数据结构完成硬件抽象。<br>其中：</p><ul><li><strong>spi_controller</strong>：描述 SoC 中的 SPI 控制器，负责操作具体硬件</li><li><strong>spi_device</strong>：描述挂载在 SPI 总线上的外设</li><li><strong>spi_driver</strong>：描述设备驱动，通过 probe 函数完成设备初始化</li><li><strong>spi_message</strong>：描述一次完整 SPI 事务</li><li><strong>spi_transfer</strong>：描述一次具体的数据交换</li></ul><p>整个 SPI 框架通过这些结构体，将设备驱动和底层硬件控制解耦，使同一个设备驱动可以运行在不同 SoC 平台上。</p><hr><h1 id="8-互动"><a href="#8-互动" class="headerlink" title="8. 互动"></a>8. 互动</h1><ol><li>spi_controller 是什么？</li><li>spi_device 是什么？</li><li>spi_driver 是什么？</li><li>spi_message 和 spi_transfer 为什么存在？</li><li>一次 SPI 传输在这些结构体之间如何流转？</li></ol>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>SPI</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux SPI</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>问题及思考</title>
    <link href="/2026/07/13/Linux%20Kernel/problem/"/>
    <url>/2026/07/13/Linux%20Kernel/problem/</url>
    
    <content type="html"><![CDATA[<h1 id="0-问题"><a href="#0-问题" class="headerlink" title="0 问题"></a>0 问题</h1><h2 id="1-内存"><a href="#1-内存" class="headerlink" title="1 内存"></a>1 内存</h2><h3 id="1-1-内存空洞是什么？外部碎片和内部碎片又是什么？"><a href="#1-1-内存空洞是什么？外部碎片和内部碎片又是什么？" class="headerlink" title="1.1 内存空洞是什么？外部碎片和内部碎片又是什么？"></a>1.1 内存空洞是什么？外部碎片和内部碎片又是什么？</h3><ol><li>内存空洞<br>首先要理解的是机器的物理地址中不一定全部对应真实 RAM 及不对应真实的物理结构，示意图</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs shell">物理地址<br>0 GB  ┌──────────────┐<br>      │ RAM          │<br>1 GB  ├──────────────┤<br>      │ 地址空洞      │  ← 没有真实内存，可能保留给设备/MMIO<br>3 GB  ├──────────────┤<br>      │ RAM          │<br>8 GB  └──────────────┘<br></code></pre></td></tr></table></figure><ol start="2"><li>外部碎片<br>外部碎片是：可用内存总量够，但空闲空间被切成了很多不连续的小块，无法满足连续分配要求。<br>示意图：</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">已用  空闲  已用  空闲  已用  空闲<br> ██    □□    ██    □□    ██    □□<br></code></pre></td></tr></table></figure><ol start="3"><li>内部碎片<br>内部碎片：顾名思义，发生在分配给对象的内存的内部，分配给对象的空间大于对象实际需要，浪费发生在已分配块内部。<br>一页是 4 KiB，某次分配必须占用一整页，但实际只用了 3 KiB，示意图：</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell">一页 4 KiB<br>┌──────────────────────┐<br>│ 实际使用 3 KiB │浪费1KiB│<br>└──────────────────────┘<br></code></pre></td></tr></table></figure><p>这 1 KiB 已经属于该分配，其他对象不能直接使用，因此是内部碎片。</p>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>内存管理</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>RK3566 BSP</title>
    <link href="/2026/07/13/rk3566/P1/"/>
    <url>/2026/07/13/rk3566/P1/</url>
    
    <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>本项目基于泰山派编写一款属于自己Linux SDK。</p><h2 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h2><h2 id="Uboot"><a href="#Uboot" class="headerlink" title="Uboot"></a>Uboot</h2><p>下载uboot源码</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">git clone https://github.com/rockchip-linux/u-boot.git<br></code></pre></td></tr></table></figure><h3 id="源码结构"><a href="#源码结构" class="headerlink" title="源码结构"></a>源码结构</h3><ol><li>soc<br>先看源码结构<br>这是 RK3566 SoC相关代码。<br>例如：</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">arch</span>/arm/mach-rockchip/<br><br>├── rk3568<br>├── rk3399<br>└── rk3588<br></code></pre></td></tr></table></figure><p>里面负责：CPU初始化、时钟、DDR、电源<br>因为 RK3566 和 RK3568 接近：<br>很多代码：<br>rk3568&#x2F;</p><ol start="2"><li><p>板级代码。<br>例如：<br>board&#x2F;rockchip&#x2F;<br>里面：<br>evb_rk3568<br>类似：<br>board&#x2F;rockchip&#x2F;evb_rk3568&#x2F;<br>负责：<br>开发板差异。<br>比如：<br>GPIO<br>LED<br>电源<br>按键</p></li><li><p>驱动代码<br>drivers&#x2F;<br>硬件驱动。<br>比如：<br>MMC<br>drivers&#x2F;mmc&#x2F;<br>对应：<br>eMMC</p></li></ol><p>UART<br>drivers&#x2F;serial&#x2F;<br>对应<br>串口输出<br>Clock<br>drivers&#x2F;clk&#x2F;<br>对应：<br>时钟</p><ol start="5"><li>cmd&#x2F;<br>U-Boot命令。<br>例如：<br>cmd&#x2F;mmc.c<br>cmd&#x2F;bootm.c<br>cmd&#x2F;load.c<br>对应：<br>&#x3D;&gt; mmc list<br>&#x3D;&gt; boot</li></ol><h3 id="编译"><a href="#编译" class="headerlink" title="编译"></a>编译</h3><p>进入源码目录，查看板级支持</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">ls configs | grep rk3566<br></code></pre></td></tr></table></figure><p>显示</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell">rk3566-ebook.config<br>rk3566-nand.config<br>rk3566.config<br></code></pre></td></tr></table></figure><p>下载rkbin文件</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs shell">git clone https://github.com/rockchip-linux/rkbin.git<br></code></pre></td></tr></table></figure><p>下载编译工具</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">gcc</span>-linaro-<span class="hljs-number">6</span>.<span class="hljs-number">3</span>.<span class="hljs-number">1</span>-<span class="hljs-number">2017</span>.<span class="hljs-number">05</span>-x86_64_arm-linux-gnueabihf.tar.xz<br><span class="hljs-attribute">gcc</span>-linaro-<span class="hljs-number">6</span>.<span class="hljs-number">3</span>.<span class="hljs-number">1</span>-<span class="hljs-number">2017</span>.<span class="hljs-number">05</span>-x86_64_aarch64-linux-gnu.tar.xz<br></code></pre></td></tr></table></figure><p>修改make.sh</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs shell">CROSS_COMPILE_ARM32=../toolchain/arm32/gcc-linaro-6.3.1-2017.05-x86_64_arm-linux-gnueabihf/bin/arm-linux-gnueabihf-<br>CROSS_COMPILE_ARM64=../toolchain/arm64/gcc-linaro-6.3.1-2017.05-x86_64_aarch64-linux-gnu/bin/aarch64-linux-gnu-<br></code></pre></td></tr></table></figure><p>编译</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs vim">./<span class="hljs-keyword">make</span>.<span class="hljs-keyword">sh</span> clean<br>./<span class="hljs-keyword">make</span>.<span class="hljs-keyword">sh</span><br></code></pre></td></tr></table></figure><h3 id="代码学习"><a href="#代码学习" class="headerlink" title="代码学习"></a>代码学习</h3><p>按照启动顺序看</p><ol><li>board_init_f()<br>位置：u-boot&#x2F;common&#x2F;board_f.c<br>作用：Uboot启动过程中前半段初始化函数，在 U-Boot 重定位之前，完成最基本的硬件和运行环境初始化，并计算后续 U-Boot 应该搬到内存的什么位置。<br>整个启动过程</li></ol><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs shell">CPU 上电 / 复位<br>      ↓<br>汇编启动代码 start.S<br>      ↓<br>board_init_f()<br>      ↓<br>执行 init_sequence_f[] 中的初始化函数<br>      ↓<br>计算 U-Boot 重定位地址<br>      ↓<br>relocate_code()<br>      ↓<br>board_init_r()<br>      ↓<br>进入完整的 U-Boot 命令行环境<br></code></pre></td></tr></table></figure><p>board_init_f() 自己并没有把所有初始化逻辑都写出来，而是按照顺序执行 init_sequence_f[] 中登记的函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">board_init_f</span><span class="hljs-params">(ulong boot_flags)</span><br>&#123;<br>  gd-&gt;flags = boot_flags;<br>  gd-&gt;have_console = <span class="hljs-number">0</span>;<br><br>  <span class="hljs-keyword">if</span> (initcall_run_list(init_sequence_f))<br>    hang();<br><br><span class="hljs-meta">#<span class="hljs-keyword">if</span> !defined(CONFIG_ARM) &amp;&amp; !defined(CONFIG_SANDBOX) &amp;&amp; \</span><br><span class="hljs-meta">    !defined(CONFIG_EFI_APP) &amp;&amp; !CONFIG_IS_ENABLED(X86_64)</span><br>  <span class="hljs-comment">/* NOTREACHED - jump_to_copy() does not return */</span><br>  hang();<br><span class="hljs-meta">#<span class="hljs-keyword">endif</span></span><br>&#125;<br></code></pre></td></tr></table></figure><p>init_sequence_f[]是一张函数指针表，里面有几个重要的初始化函数：</p><ul><li>dram_init()：用于初始化或探测 DRAM，并记录内存容量</li><li>timer_init()：初始化早期定时器，让 U-Boot 能够获得时间基准</li><li>serial_init()：初始化早期串口，主要用于输出启动日志</li></ul><ol start="2"><li>board_init_r()<br>位置：common&#x2F;board_r.c<br>作用：完成设备、存储、环境变量、网络等完整初始化，最终执行 bootcmd 或进入命令行。<br>简化代码：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">board_init_r</span><span class="hljs-params">(<span class="hljs-type">gd_t</span> *new_gd, ulong dest_addr)</span><br>&#123;<br>    gd = new_gd;<br><br>    <span class="hljs-keyword">if</span> (initcall_run_list(init_sequence_r))<br>        hang();<br><br>    hang();<br>&#125;<br></code></pre></td></tr></table></figure><p>与 board_init_f() 类似，真正的初始化工作主要由函数指针数组完成：init_sequence_r[]<br>里面有几个重要函数</p><ul><li>initr_mmc：MMC 初始化主要负责准备，SD 卡、eMMCMMC 控制器、对应块设备</li></ul>]]></content>
    
    
    <categories>
      
      <category>嵌入式</category>
      
      <category>RK3566</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Memory Management</title>
    <link href="/2026/07/13/Linux%20Kernel/Memory%20Management/%E7%90%86%E8%AE%BA/"/>
    <url>/2026/07/13/Linux%20Kernel/Memory%20Management/%E7%90%86%E8%AE%BA/</url>
    
    <content type="html"><![CDATA[<h1 id="理论"><a href="#理论" class="headerlink" title="理论"></a>理论</h1>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>内存管理</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>进程优先级</title>
    <link href="/2026/07/12/Linux%20Kernel/process_management/%E8%BF%9B%E7%A8%8B%E4%BC%98%E5%85%88%E7%BA%A7/"/>
    <url>/2026/07/12/Linux%20Kernel/process_management/%E8%BF%9B%E7%A8%8B%E4%BC%98%E5%85%88%E7%BA%A7/</url>
    
    <content type="html"><![CDATA[<h1 id="进程优先级"><a href="#进程优先级" class="headerlink" title="进程优先级"></a>进程优先级</h1><blockquote><p><strong>关键结论</strong></p><ul><li>Linux 内核使用数值 <strong>小即高优先级</strong> 的规则：Deadline（-1）&gt; 实时（0–98）&gt; 普通（100–139）。</li><li><code>static_prio = nice + 120</code>，对普通任务生效；实时任务只看 <code>rt_priority</code>（1–99）。</li><li>调度器最终使用的是 <code>p-&gt;prio</code>（动态优先级），它在优先级继承（PI）场景中可能与<br><code>normal_prio</code> 不同。</li><li><code>normal_prio</code> 根据 policy 统一映射到内核内部优先级：Deadline → -1，RT → <code>100 - 1 - rt_prio</code>，<br>普通 → <code>nice + 120</code>。</li></ul></blockquote><h2 id="从-task-struct-看优先级字段"><a href="#从-task-struct-看优先级字段" class="headerlink" title="从 task_struct 看优先级字段"></a>从 task_struct 看优先级字段</h2><p>Linux 内核在 <code>task_struct</code> 中维护了多个优先级相关字段（<br><a href="https://elixir.bootlin.com/linux/v6.14.3/source/include/linux/sched.h"><code>include/linux/sched.h</code></a>）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">int</span>             prio;           <span class="hljs-comment">// 动态优先级——调度器实际使用</span><br><span class="hljs-type">int</span>             static_prio;    <span class="hljs-comment">// 静态优先级——普通任务的 nice 内核表示</span><br><span class="hljs-type">int</span>             normal_prio;    <span class="hljs-comment">// 归一化优先级——由 policy 与静态参数计算</span><br><span class="hljs-type">unsigned</span> <span class="hljs-type">int</span>    rt_priority;    <span class="hljs-comment">// 实时优先级——仅对 SCHED_FIFO / SCHED_RR 有效</span><br><span class="hljs-type">unsigned</span> <span class="hljs-type">int</span>    policy;         <span class="hljs-comment">// 调度策略</span><br></code></pre></td></tr></table></figure><p>这几个字段各司其职。理解它们之间的关系，是掌握 Linux 调度行为的前提。</p><h2 id="调度策略与优先级的整体关系"><a href="#调度策略与优先级的整体关系" class="headerlink" title="调度策略与优先级的整体关系"></a>调度策略与优先级的整体关系</h2><p>内核通过 <code>policy</code> 字段区分任务的调度类。每种策略使用的优先级参数不同，<br>内核内部优先级数值也不同。</p><h3 id="调度策略一览"><a href="#调度策略一览" class="headerlink" title="调度策略一览"></a>调度策略一览</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">/*</span><br><span class="hljs-comment"> * Scheduling policies</span><br><span class="hljs-comment"> */</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_NORMAL        0   <span class="hljs-comment">// 普通，CFS 调度（也称 SCHED_OTHER）</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_FIFO          1   <span class="hljs-comment">// 实时 FIFO，无时间片</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_RR            2   <span class="hljs-comment">// 实时轮转，同优先级按时间片轮转</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_BATCH         3   <span class="hljs-comment">// 普通批处理，不强调交互</span></span><br><span class="hljs-comment">/* SCHED_ISO: reserved but not implemented yet */</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_IDLE          5   <span class="hljs-comment">// 极低优先级，系统空闲时才运行</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_DEADLINE      6   <span class="hljs-comment">// Deadline 实时，按截止期限调度</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> SCHED_EXT           7   <span class="hljs-comment">// 可扩展（sched_ext，BPF 调度器）</span></span><br></code></pre></td></tr></table></figure><p>策略宏定义于 <a href="https://elixir.bootlin.com/linux/v6.14.3/source/include/uapi/linux/sched.h"><code>include/uapi/linux/sched.h</code></a>。</p><h3 id="策略-→-优先级依据-→-内核内部优先级映射"><a href="#策略-→-优先级依据-→-内核内部优先级映射" class="headerlink" title="策略 → 优先级依据 → 内核内部优先级映射"></a>策略 → 优先级依据 → 内核内部优先级映射</h3><table><thead><tr><th>策略</th><th>类型</th><th>调度类</th><th>优先级依据</th><th>内核 prio 范围</th><th>说明</th></tr></thead><tbody><tr><td><code>SCHED_DEADLINE</code></td><td>Deadline 实时</td><td><code>dl_sched_class</code></td><td><code>runtime</code> &#x2F; <code>deadline</code> &#x2F; <code>period</code></td><td><strong>-1</strong></td><td>最高优先级；不使用 <code>nice</code> 或 <code>rt_priority</code></td></tr><tr><td><code>SCHED_FIFO</code></td><td>实时</td><td><code>rt_sched_class</code></td><td><code>rt_priority</code>（1–99）</td><td><strong>0–98</strong></td><td>先进先出；运行至阻塞&#x2F;让出&#x2F;被抢占</td></tr><tr><td><code>SCHED_RR</code></td><td>实时</td><td><code>rt_sched_class</code></td><td><code>rt_priority</code>（1–99）</td><td><strong>0–98</strong></td><td>实时轮转；同优先级线程按时间片轮流</td></tr><tr><td><code>SCHED_NORMAL</code></td><td>普通</td><td><code>fair_sched_class</code></td><td><code>nice</code>（-20–+19）</td><td><strong>100–139</strong></td><td>默认策略；权重由 <code>nice</code> 决定</td></tr><tr><td><code>SCHED_BATCH</code></td><td>普通</td><td><code>fair_sched_class</code></td><td><code>nice</code>（-20–+19）</td><td><strong>100–139</strong></td><td>面向批处理；唤醒逻辑与 NORMAL 不同</td></tr><tr><td><code>SCHED_IDLE</code></td><td>普通</td><td><code>fair_sched_class</code></td><td>Idle 权重</td><td><strong>100–139</strong></td><td>极低优先级；仅在系统空闲时运行</td></tr><tr><td><code>SCHED_EXT</code></td><td>可扩展</td><td><code>ext_sched_class</code></td><td>由 BPF 定义</td><td><em>BPF 决定</em></td><td><code>sched_ext</code> 可编程调度（Linux 6.12+）</td></tr></tbody></table><blockquote><p><strong>核心规则：</strong> 内核内部优先级数值越小，调度优先级越高。<br>在任何 CPU 上，一个可运行的 Deadline 或 RT 任务会立即抢占普通任务。</p></blockquote><h3 id="三类任务的核心行为对比"><a href="#三类任务的核心行为对比" class="headerlink" title="三类任务的核心行为对比"></a>三类任务的核心行为对比</h3><table><thead><tr><th>维度</th><th>普通任务（CFS）</th><th>FIFO&#x2F;RR 实时任务</th><th>Deadline 任务</th></tr></thead><tbody><tr><td>优先级参数</td><td><code>nice</code>（-20–+19）</td><td><code>rt_priority</code>（1–99）</td><td><code>runtime</code> &#x2F; <code>deadline</code> &#x2F; <code>period</code></td></tr><tr><td>内核 prio</td><td>100–139</td><td>0–98</td><td>-1</td></tr><tr><td>时间片</td><td>有（CFS 按权重动态分配）</td><td>FIFO 无、RR 有固定时间片</td><td>按 <code>runtime</code> 预算</td></tr><tr><td>抢占规则</td><td>被 RT&#x2F;DL 抢占；CFS 内部按 vruntime</td><td>更高 <code>rt_priority</code> 抢占；同优先级 FIFO 不抢占</td><td>Earliest Deadline First (EDF)</td></tr><tr><td>用户态设置</td><td><code>nice(2)</code> &#x2F; <code>setpriority(2)</code></td><td><code>sched_setscheduler(2)</code></td><td><code>sched_setattr(2)</code></td></tr><tr><td>适用场景</td><td>通用应用程序</td><td>硬实时响应（IRQ 线程、音频）</td><td>周期性实时任务（视频编解码）</td></tr></tbody></table><h2 id="静态优先级-static-prio"><a href="#静态优先级-static-prio" class="headerlink" title="静态优先级 static_prio"></a>静态优先级 <code>static_prio</code></h2><p><code>static_prio</code> 是普通线程 nice 值的内核内部表示。nice 值在 <strong>-20 到 +19</strong> 之间，<br>nice 越大表示线程越”友好”——越容易让出 CPU。</p><p>内核内部普通优先级范围是 <strong>100 到 139</strong>（值越大，调度优先级越低）。<br>转换关系定义在 <a href="https://elixir.bootlin.com/linux/v6.14.3/source/include/linux/sched/prio.h"><code>include/linux/sched/prio.h</code></a>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">define</span> MAX_NICE         19</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> MIN_NICE        -20</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> NICE_WIDTH      (MAX_NICE - MIN_NICE + 1)   <span class="hljs-comment">// = 40</span></span><br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> MAX_USER_RT_PRIO 100</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> MAX_RT_PRIO      MAX_USER_RT_PRIO           <span class="hljs-comment">// = 100</span></span><br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> MAX_PRIO         (MAX_RT_PRIO + NICE_WIDTH)  <span class="hljs-comment">// = 140</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> DEFAULT_PRIO     (MAX_RT_PRIO + NICE_WIDTH / 2)  <span class="hljs-comment">// = 120</span></span><br><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> NICE_TO_PRIO(nice)  ((nice) + DEFAULT_PRIO)</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> PRIO_TO_NICE(prio)  ((prio) - DEFAULT_PRIO)</span><br></code></pre></td></tr></table></figure><p>展开即：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ini"><span class="hljs-attr">static_prio</span> = nice + <span class="hljs-number">120</span><br></code></pre></td></tr></table></figure><p><strong>示例：</strong></p><table><thead><tr><th>nice 值</th><th><code>static_prio</code></th><th>含义</th></tr></thead><tbody><tr><td>-20</td><td>100</td><td>普通任务中的最高调度优先级</td></tr><tr><td>-10</td><td>110</td><td>高于默认</td></tr><tr><td>0</td><td>120 (<code>DEFAULT_PRIO</code>)</td><td>默认优先级</td></tr><tr><td>10</td><td>130</td><td>低于默认</td></tr><tr><td>19</td><td>139</td><td>普通任务中的最低调度优先级</td></tr></tbody></table><blockquote><p><code>static_prio</code> 在进程生命周期内通常不变，仅可通过 <code>nice(2)</code> &#x2F; <code>setpriority(2)</code> 系统调用修改。<br>实时任务不使用 <code>static_prio</code>（其值在 RT 任务中无调度意义）。</p></blockquote><h2 id="实时优先级-rt-priority"><a href="#实时优先级-rt-priority" class="headerlink" title="实时优先级 rt_priority"></a>实时优先级 <code>rt_priority</code></h2><p><code>rt_priority</code> 仅对 <code>SCHED_FIFO</code> 和 <code>SCHED_RR</code> 生效。用户空间取值范围为 <strong>1 到 99</strong>（<br><a href="https://man7.org/linux/man-pages/man7/sched.7.html">sched(7) man-page</a>），<br>0 为系统保留。值越大，实时优先级越高。</p><p>可通过 <code>sched_get_priority_min(2)</code> &#x2F; <code>sched_get_priority_max(2)</code> 查询系统支持范围：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// SCHED_FIFO 返回 1；SCHED_RR 返回 1</span><br><span class="hljs-type">int</span> min = sched_get_priority_min(SCHED_FIFO);<br><span class="hljs-comment">// SCHED_FIFO 返回 99；SCHED_RR 返回 99</span><br><span class="hljs-type">int</span> max = sched_get_priority_max(SCHED_FIFO);<br></code></pre></td></tr></table></figure><blockquote><p><strong>兼容性提醒：</strong> POSIX.1 仅要求实现提供至少 32 个实时优先级级别。<br>可移植程序应通过上述系统调用动态获取实际范围，而非硬编码 1–99。</p></blockquote><p>实时任务的 <code>rt_priority</code> 在 CFS 调度器中不参与 <code>vruntime</code> 计算——它的优先级<br>直接来自调度类的排序（RT &gt; CFS），而非权重分配。</p><h2 id="归一化优先级-normal-prio"><a href="#归一化优先级-normal-prio" class="headerlink" title="归一化优先级 normal_prio"></a>归一化优先级 <code>normal_prio</code></h2><p><code>normal_prio</code> 将不同策略的优先级参数统一映射到内核内部优先级数值。<br>源码位于 <a href="https://elixir.bootlin.com/linux/v6.14.3/source/kernel/sched/core.c"><code>kernel/sched/core.c</code></a>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">static</span> <span class="hljs-keyword">inline</span> <span class="hljs-type">int</span> __normal_prio(<span class="hljs-type">int</span> policy, <span class="hljs-type">int</span> rt_prio, <span class="hljs-type">int</span> nice)<br>&#123;<br>    <span class="hljs-type">int</span> prio;<br><br>    <span class="hljs-keyword">if</span> (dl_policy(policy))<br>        prio = MAX_DL_PRIO - <span class="hljs-number">1</span>;             <span class="hljs-comment">// = -1</span><br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rt_policy(policy))<br>        prio = MAX_RT_PRIO - <span class="hljs-number">1</span> - rt_prio;   <span class="hljs-comment">// = 99 - rt_prio</span><br>    <span class="hljs-keyword">else</span><br>        prio = NICE_TO_PRIO(nice);          <span class="hljs-comment">// = nice + 120</span><br><br>    <span class="hljs-keyword">return</span> prio;<br>&#125;<br><br><span class="hljs-type">static</span> <span class="hljs-keyword">inline</span> <span class="hljs-type">int</span> <span class="hljs-title function_">normal_prio</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> task_struct *p)</span><br>&#123;<br>    <span class="hljs-keyword">return</span> __normal_prio(p-&gt;policy, p-&gt;rt_priority,<br>                         PRIO_TO_NICE(p-&gt;static_prio));<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>映射规则：</strong></p><table><thead><tr><th>策略</th><th>输入参数</th><th>计算公式</th><th><code>normal_prio</code> 范围</th></tr></thead><tbody><tr><td><code>SCHED_DEADLINE</code></td><td>—</td><td><code>MAX_DL_PRIO - 1</code></td><td><strong>-1</strong></td></tr><tr><td><code>SCHED_FIFO</code> &#x2F; <code>SCHED_RR</code></td><td><code>rt_priority</code> (1–99)</td><td><code>99 - rt_priority</code></td><td>0（最高 RT）– 98（最低 RT）</td></tr><tr><td><code>SCHED_NORMAL</code> &#x2F; <code>SCHED_BATCH</code> &#x2F; <code>SCHED_IDLE</code></td><td><code>nice</code> (-20–+19)</td><td><code>nice + 120</code></td><td>100（nice&#x3D;-20）– 139（nice&#x3D;+19）</td></tr></tbody></table><p><strong>计算示例：</strong></p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-comment">// SCHED_FIFO, rt_priority=99 → normal_prio = 99-99 = 0  (最高 RT)</span><br><span class="hljs-comment">// SCHED_FIFO, rt_priority=1  → normal_prio = 99-1  = 98 (最低 RT)</span><br><span class="hljs-comment">// SCHED_NORMAL, nice=-10     → normal_prio = 110        (高于默认)</span><br><span class="hljs-comment">// SCHED_NORMAL, nice=5       → normal_prio = 125        (低于默认)</span><br><span class="hljs-comment">// SCHED_DEADLINE             → normal_prio = -1         (高于一切)</span><br></code></pre></td></tr></table></figure><blockquote><p><code>normal_prio</code> 是任务”本来的”优先级——不含任何临时调整。<br>它由策略和静态参数计算得到，不随运行时状态变化。</p></blockquote><h2 id="动态优先级-prio"><a href="#动态优先级-prio" class="headerlink" title="动态优先级 prio"></a>动态优先级 <code>prio</code></h2><p><code>prio</code> 是调度器在做调度决策时<strong>实际使用的优先级</strong>。它在大多数时候等于 <code>normal_prio</code>，<br>但在特定情况下会被临时修改。设置 <code>prio</code> 的核心函数是 <code>effective_prio()</code>（<br><a href="https://elixir.bootlin.com/linux/v6.14.3/source/kernel/sched/core.c"><code>kernel/sched/core.c</code></a>）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-type">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">effective_prio</span><span class="hljs-params">(<span class="hljs-keyword">struct</span> task_struct *p)</span><br>&#123;<br>    p-&gt;normal_prio = normal_prio(p);<br>    <span class="hljs-comment">/*</span><br><span class="hljs-comment">     * 如果当前动态优先级已经处于 RT 范围（说明正在享受</span><br><span class="hljs-comment">     * 优先级继承带来的临时提升），则保留之；否则退回 normal_prio。</span><br><span class="hljs-comment">     */</span><br>    <span class="hljs-keyword">if</span> (!rt_prio(p-&gt;prio))<br>        <span class="hljs-keyword">return</span> p-&gt;normal_prio;<br>    <span class="hljs-keyword">return</span> p-&gt;prio;<br>&#125;<br></code></pre></td></tr></table></figure><p>一般调用路径：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs c">p-&gt;prio = effective_prio(p);<br></code></pre></td></tr></table></figure><h3 id="什么时候-prio-≠-normal-prio？"><a href="#什么时候-prio-≠-normal-prio？" class="headerlink" title="什么时候 prio ≠ normal_prio？"></a>什么时候 <code>prio ≠ normal_prio</code>？</h3><p>最常见的情况是 <strong>优先级继承（Priority Inheritance, PI）</strong>。</p><p><strong>场景：</strong> 一个高优先级的 RT 任务（rt_priority&#x3D;80）尝试获取互斥锁，但该锁被一个<br>低优先级的普通任务（nice&#x3D;0, prio&#x3D;120）持有。如果内核不干预，RT 任务会被普通任务<br>无限期阻塞——这就是著名的<strong>优先级反转</strong>问题。</p><p><strong>内核的处理：</strong> <code>rt_mutex</code> 实现会<strong>临时提升</strong>锁持有者的 <code>prio</code> 到等待者中最高<br>RT 优先级的水平。在上述场景中，普通任务的 <code>prio</code> 会被临时提升到 RT 范围（例如 <code>prio=19</code>），<br>使其能尽快执行完临界区并释放锁。<code>effective_prio()</code> 中的 <code>rt_prio(p-&gt;prio)</code> 检查<br>正是用来保留这种临时提升的：如果 <code>prio</code> 已经在 RT 范围，就不要降回 <code>normal_prio</code>。</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs abnf">PI 提升前：普通任务 prio <span class="hljs-operator">=</span> <span class="hljs-number">120</span> <span class="hljs-operator">=</span> normal_prio<br>PI 提升后：普通任务 prio <span class="hljs-operator">=</span> <span class="hljs-number">19</span>  ≠ normal_prio (<span class="hljs-number">120</span>)<br>——任务获得了临时的 RT 级调度优先级<br></code></pre></td></tr></table></figure><blockquote><p>Linux 的 PI 实现细节见内核文档<br><a href="https://docs.kernel.org/locking/rt-mutex.html"><code>Documentation/locking/rt-mutex.rst</code></a> 和<br><a href="https://elixir.bootlin.com/linux/v6.14.3/source/kernel/locking/rtmutex.c"><code>kernel/locking/rtmutex.c</code></a>。</p></blockquote><h2 id="优先级转换全景"><a href="#优先级转换全景" class="headerlink" title="优先级转换全景"></a>优先级转换全景</h2><p>下图将 <code>nice</code>、<code>rt_priority</code>、<code>policy</code> 到内核内部 <code>prio</code> 的完整映射关系汇总在一处：</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.39lve7jjva.webp"></p><p>图中三条色带从上到下对应三种优先级域。<strong>Deadline 任务始终优先于 RT，<br>RT 始终优先于普通。</strong> 绿色区域的普通任务中，<code>static_prio</code> 和 <code>prio</code><br>通常相等，但在优先级继承发生时 <code>prio</code> 会临时上浮到 RT 区域。</p><h2 id="动态优先级的运行时变化"><a href="#动态优先级的运行时变化" class="headerlink" title="动态优先级的运行时变化"></a>动态优先级的运行时变化</h2><p>除了 PI，还有两种常见情况下 <code>prio</code> 会发生变化：</p><ol><li><p><strong><code>nice(2)</code> 系统调用</strong>：修改 <code>static_prio</code> 后，内核重新计算<br><code>normal_prio</code> 并调用 <code>effective_prio()</code> 更新 <code>prio</code>。</p></li><li><p><strong><code>sched_setscheduler(2)</code></strong>：改变 <code>policy</code> 和&#x2F;或 <code>rt_priority</code> 后，<br><code>normal_prio()</code> 重新计算，<code>prio</code> 随之更新。</p></li></ol><p>以下是一个 <code>SCHED_FIFO</code> 的简单示例（仅示意，省略错误处理）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;sched.h&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdio.h&gt;</span></span><br><br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">sched_param</span> <span class="hljs-title">param</span> =</span> &#123; .sched_priority = <span class="hljs-number">80</span> &#125;;<br>sched_setscheduler(<span class="hljs-number">0</span>, SCHED_FIFO, &amp;param);<br><span class="hljs-comment">// 此后：</span><br><span class="hljs-comment">//   policy      = SCHED_FIFO</span><br><span class="hljs-comment">//   rt_priority = 80</span><br><span class="hljs-comment">//   normal_prio = 99 - 80 = 19</span><br><span class="hljs-comment">//   prio        = 19（在 RT 范围内）</span><br></code></pre></td></tr></table></figure><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="1-为什么-rt-priority-只到-99-而不是更大？"><a href="#1-为什么-rt-priority-只到-99-而不是更大？" class="headerlink" title="1. 为什么 rt_priority 只到 99 而不是更大？"></a>1. 为什么 <code>rt_priority</code> 只到 99 而不是更大？</h3><p><code>MAX_USER_RT_PRIO</code> 定义为 100，用户可用范围为 1–99（0 保留给内核）。<br>这个值自 Linux 早期版本即固定，内部优先级空间（0–139）总共 140 个级别<br>（<code>MAX_PRIO = MAX_RT_PRIO + NICE_WIDTH = 100 + 40</code>），从设计上区分了<br>RT 和普通任务的优先级区间。</p><h3 id="2-实时任务会不会饿死普通任务？"><a href="#2-实时任务会不会饿死普通任务？" class="headerlink" title="2. 实时任务会不会饿死普通任务？"></a>2. 实时任务会不会饿死普通任务？</h3><p>会。高 <code>rt_priority</code> 的 CPU 密集型实时任务会持续占用 CPU，<br>导致所有普通任务无法运行。这是设计的取舍——实时调度策略假定<br><strong>实时任务的行为是可控且受限的</strong>。在实践中有两个保护机制：</p><ul><li><strong><code>RLIMIT_RTTIME</code></strong>（<a href="https://man7.org/linux/man-pages/man2/getrlimit.2.html"><code>getrlimit(2)</code></a>）：<br>限制实时任务连续运行时间，超出后发送 <code>SIGXCPU</code> 信号。</li><li><strong>调度器节流（RT throttling）</strong>：<code>/proc/sys/kernel/sched_rt_runtime_us</code><br>限制每周期 RT 任务的总运行时间（默认 RT 占 95%，留 5% 给普通任务）。</li></ul><h3 id="3-SCHED-BATCH-和-SCHED-NORMAL-有什么区别？"><a href="#3-SCHED-BATCH-和-SCHED-NORMAL-有什么区别？" class="headerlink" title="3. SCHED_BATCH 和 SCHED_NORMAL 有什么区别？"></a>3. <code>SCHED_BATCH</code> 和 <code>SCHED_NORMAL</code> 有什么区别？</h3><p>两者同属 <code>fair_sched_class</code>，使用相同的 <code>nice</code> 权重体系。区别在于唤醒行为：<br><code>SCHED_BATCH</code> 任务的唤醒延迟更长，调度器会认为它们对交互响应不敏感，<br>从而减少抢占和迁移开销。适合编译任务、日志处理等批处理负载。</p><h3 id="4-Deadline-调度不使用-rt-priority，那优先级怎么确定？"><a href="#4-Deadline-调度不使用-rt-priority，那优先级怎么确定？" class="headerlink" title="4. Deadline 调度不使用 rt_priority，那优先级怎么确定？"></a>4. Deadline 调度不使用 <code>rt_priority</code>，那优先级怎么确定？</h3><p><code>SCHED_DEADLINE</code> 通过三个参数控制调度：</p><ul><li><strong><code>sched_runtime</code></strong>：每个周期内的最大执行时间</li><li><strong><code>sched_deadline</code></strong>：任务必须在此相对截止期限内完成</li><li><strong><code>sched_period</code></strong>：重复周期</li></ul><p>调度器使用 <strong>Earliest Deadline First (EDF)</strong> 算法：截止期限越近，<br>调度优先级越高。约束条件为 <code>runtime ≤ deadline ≤ period</code>。<br>详见内核文档 <a href="https://docs.kernel.org/scheduler/sched-deadline.html"><code>Documentation/scheduler/sched-deadline.rst</code></a>。</p><h3 id="5-SCHED-IDLE-和-nice-19-哪个优先级更低？"><a href="#5-SCHED-IDLE-和-nice-19-哪个优先级更低？" class="headerlink" title="5. SCHED_IDLE 和 nice=19 哪个优先级更低？"></a>5. <code>SCHED_IDLE</code> 和 <code>nice=19</code> 哪个优先级更低？</h3><p><code>SCHED_IDLE</code> 比 <code>nice=19</code> 更低。<code>SCHED_IDLE</code> 使用特殊的 Idle 权重，<br>仅在<strong>没有其他可运行任务</strong>时才获得 CPU 时间。<code>nice=19</code> 的普通任务<br>至少还会在 CFS 中获得极小的时间片。直观排序如下：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">Deadline</span> &gt; RT(<span class="hljs-number">0</span>-<span class="hljs-number">98</span>) &gt; nice=-<span class="hljs-number">20</span>(prio=<span class="hljs-number">100</span>) &gt; ... &gt; nice=<span class="hljs-number">19</span>(prio=<span class="hljs-number">139</span>) &gt; SCHED_IDLE<br></code></pre></td></tr></table></figure><hr><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>Linux 的优先级体系由四个核心字段构成一个层次分明的整体：</p><ul><li><strong><code>static_prio</code></strong>：面向普通任务，由 <code>nice</code> 值线性映射（<code>nice + 120</code>），范围 100–139。</li><li><strong><code>rt_priority</code></strong>：面向 RT 任务，用户可设 1–99，大即高优先级。</li><li><strong><code>normal_prio</code></strong>：将 <code>policy</code> 与上述静态参数统一定价到内核内部优先级（-1 到 139）。</li><li><strong><code>prio</code></strong>：调度器实际使用的动态优先级。通常等于 <code>normal_prio</code>，但在优先级继承等<br>场景下会临时偏离——这是内核处理优先级反转的核心机制。</li></ul><p>在同一个 CPU 上，调度类的选择优先于内部优先级数值：Deadline 调度类先于 RT 调度类，<br>RT 调度类先于 CFS 调度类。只有在调度类内部，<code>prio</code> 的数值比较才决定谁先运行。</p><p><img src="https://github.com/CJ0510/picx-images-hosting/raw/master/image.et78bzv3i.webp" alt="优先级模型"></p><hr><h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><ul><li><a href="https://elixir.bootlin.com/linux/v6.14.3/source/include/linux/sched/prio.h"><code>include/linux/sched/prio.h</code></a> — 优先级常量和转换宏（<code>MAX_RT_PRIO</code>, <code>MAX_PRIO</code>, <code>DEFAULT_PRIO</code>, <code>NICE_TO_PRIO</code> 等）</li><li><a href="https://elixir.bootlin.com/linux/v6.14.3/source/kernel/sched/core.c"><code>kernel/sched/core.c</code></a> — <code>effective_prio()</code>, <code>normal_prio()</code>, <code>__normal_prio()</code> 实现</li><li><a href="https://elixir.bootlin.com/linux/v6.14.3/source/include/uapi/linux/sched.h"><code>include/uapi/linux/sched.h</code></a> — 调度策略宏定义（<code>SCHED_NORMAL</code>, <code>SCHED_FIFO</code> 等）</li><li><a href="https://man7.org/linux/man-pages/man7/sched.7.html"><code>sched(7)</code> man-page</a> — 调度策略和优先级 API 的用户空间文档</li><li><a href="https://man7.org/linux/man-pages/man2/sched_setattr.2.html"><code>sched_setattr(2)</code> man-page</a> — <code>SCHED_DEADLINE</code> 参数说明</li><li><a href="https://docs.kernel.org/locking/rt-mutex.html"><code>Documentation/locking/rt-mutex.rst</code></a> — 优先级继承设计文档</li><li><a href="https://docs.kernel.org/scheduler/sched-deadline.html"><code>Documentation/scheduler/sched-deadline.rst</code></a> — Deadline 调度类文档</li><li><a href="https://zhuanlan.zhihu.com/p/1884171139749741899">性能优化基本功（一）：掌握 Linux 和 Android 线程优先级</a> — 应用视角的优先级解读</li><li><a href="https://elixir.bootlin.com/linux/v6.14.3/source">Linux 内核源码 (v6.14.3)</a> — 所有源码引用均在 Bootlin Elixir 可查</li></ul>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
      <category>进程管理</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
      <tag>内核</tag>
      
      <tag>进程管理</tag>
      
      <tag>进程优先级</tag>
      
      <tag>nice</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Linux-Kernel 0 资料整理</title>
    <link href="/2025/09/07/Linux%20Kernel/ziliao/"/>
    <url>/2025/09/07/Linux%20Kernel/ziliao/</url>
    
    <content type="html"><![CDATA[<h1 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h1><p><a href="https://github.com/0xAX/linux-insides?tab=readme-ov-file">linux-insides</a><br><a href="https://github.com/linux-kernel-labs-zh/docs-linux-kernel-labs-zh-cn">docs-linux-kernel-labs-zh-cn</a><br><a href="http://www.wowotech.net/">蜗窝科技</a><br><a href="https://elixir.bootlin.com/linux/v6.14.3/source">Linux Kernel Source</a><br><a href="https://lwn.net/">lwn</a><br>Linux Kernel Programming: A comprehensive and practical guide to kernel internals, writing modules, and kernel synchronization 2nd Edition,<br><a href="https://github.com/lichuang/awesome-linux-system-cn">awesome-linux-system-cn</a><br><a href="https://www.iliuqi.com/">云栖梦泽</a><br><a href="https://www.brendangregg.com/linuxperf.html">Brendan Gregg 工具链</a></p>]]></content>
    
    
    <categories>
      
      <category>Linux Kernel</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Linux</tag>
      
      <tag>内核</tag>
      
      <tag>资料</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
